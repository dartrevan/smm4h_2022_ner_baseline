1. To run the baseline solution install the requirements:
```bash
pip install -r requirements.txt
```
2. Set the DATADIR and OUTPUT_DIR variables in run_train_predict script and run it:
```bash
./run_train_predict.sh
```